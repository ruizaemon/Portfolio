---
import BaseLayout from "./BaseLayout.astro";

interface Props {
  title: string;
}

const { title } = Astro.props;
---

<BaseLayout title={title}>
  <div class="post-list-container">
    <h1 class="post-list-title animate-fadeInUp">{title}</h1>
    <div class="post-list-content">
      <slot />
    </div>
  </div>
</BaseLayout>

<style>
  .post-list-title {
    font-size: 2rem;
    font-weight: 600;
    color: var(--color-text);
    margin-block: 0.83em; /* match the h2 default this heading had before becoming an h1 */
  }

  .post-list-content {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
  }

  @media (min-width: 768px) {
    /* .post-list-title {
      margin-bottom: 3rem;
    } */
    
    .post-list-content {
      gap: 2rem;
    }
  }
</style>
